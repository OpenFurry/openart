security {

	// see DefaultSecurityConfig.groovy for all settable/overridable properties

	active = true

	loginUserDomainClass = "openfurry.Person"
	authorityDomainClass = "openfurry.Role"
	requestMapClass = "openfurry.Requestmap"
}
